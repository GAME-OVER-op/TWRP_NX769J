# Keep recovery booting while the decrypt stack is being prepared.
# Prevent keystore2 from starting too early, then start it only after qseecomd is running.

on init
    stop keystore2

on property:init.svc.vendor.qseecomd=running
    start keystore2
