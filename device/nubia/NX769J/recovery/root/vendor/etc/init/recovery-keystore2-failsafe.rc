# Keep recovery booting even if the decrypt stack is late or unavailable.
# Stop keystore2 once during boot to avoid tight crash loops before qseecomd/keymint are ready.
# Start it back after qseecomd is alive.

on boot
    stop keystore2

on property:init.svc.vendor.qseecomd=running
    start keystore2
